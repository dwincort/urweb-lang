# Source files (compiled output is in out/)
src/**
*.ts

# TypeScript config
tsconfig.json

# Note: node_modules is intentionally NOT ignored — vsce bundles production
# dependencies (vscode-languageclient) and prunes devDependencies automatically.

# Development files
.vscode/**
.vscode-test/**

# Git
.git/**
.gitignore

# Build artifacts
*.vsix

# Editor files
*.swp
*.swo
*~
